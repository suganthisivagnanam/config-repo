server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tasks1
    username: root
    password: root 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
 
